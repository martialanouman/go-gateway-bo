Native select with the Passerelle chevron; used for scopes, strategies, windows and group filters.

```jsx
<Select size="sm" placeholder="Tous les groupes" options={[{value:'ci',label:'Banques CI'}]} />
<Select options={['weighted','failover_priority','round_robin']} />
```
