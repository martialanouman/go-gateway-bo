32px text input; `sm` (26px) inside filter bars and table cells.

```jsx
<Input icon="search" placeholder="Rechercher un MSISDN, un ID de trace…" />
<Input mono placeholder="+225XXXXXXXX" />
```

Any operator-typed identifier uses `mono`.
