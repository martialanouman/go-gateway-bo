Immediate on/off of a live capability — `query_sm`/`cancel_sm`, channel enablement, alert rule status.

```jsx
<Switch label="query_sm autorisé" checked onChange={toggle} />
```

Switches apply immediately; anything needing Save uses a Checkbox instead.
