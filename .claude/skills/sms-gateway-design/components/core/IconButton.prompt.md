Square icon-only action for toolbars, table rows, card headers and modal close.

```jsx
<IconButton icon="x" label="Fermer" />
<IconButton icon="refresh-cw" label="Rafraîchir" variant="secondary" />
```

`label` is mandatory — icon-only controls in the console always carry a tooltip.
