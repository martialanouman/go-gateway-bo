# 049 — `pgx` nu contre `sqlc` : décision **confirmée**, avec un déclencheur écrit qui la rouvrirait

> **Porteur :** **sans porteur** — un déclencheur mesurable, pas une step.

## Ce qu'elle coûte si elle dure

« Ce jour-là, `sqlc` reprend l'avantage et la décision se rouvre » — le déclencheur est un événement de mesure, « un `Scan` dont la mutation d'interversion de deux champs de même type reste verte ». Le premier critère était un proxy, reconnu tel.
